<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kkdugi.core.code.mapper.CodeBaseMapper">

  <!-- CodeBase는 record라 setter가 없다. <constructor>로 canonical
       생성자를 통해 매핑한다(필드 순서: id, parentId, code, extra1..5,
       level, path, sort, use, createdAt, createdId, updatedAt, updatedId). -->
  <resultMap id="codeBaseResultMap" type="kkdugi.core.code.models.CodeBase">
    <constructor>
      <idArg column="code_id" javaType="java.lang.String"/>
      <arg column="code_parent_id" javaType="java.lang.String"/>
      <arg column="code_val" javaType="java.lang.String"/>
      <arg column="etc_val1" javaType="java.lang.String"/>
      <arg column="etc_val2" javaType="java.lang.String"/>
      <arg column="etc_val3" javaType="java.lang.String"/>
      <arg column="etc_val4" javaType="java.lang.String"/>
      <arg column="etc_val5" javaType="java.lang.String"/>
      <arg column="code_lvl" javaType="java.lang.Integer"/>
      <arg column="code_path" javaType="java.lang.String"/>
      <arg column="sort_seq" javaType="java.lang.Integer"/>
      <arg column="use_yn" javaType="java.lang.String"/>
      <arg column="reg_dtm" javaType="java.time.LocalDateTime"/>
      <arg column="reg_id" javaType="java.lang.String"/>
      <arg column="upd_dtm" javaType="java.time.LocalDateTime"/>
      <arg column="upd_id" javaType="java.lang.String"/>
    </constructor>
  </resultMap>

  <sql id="columns">
<![CDATA[
code_id, code_parent_id, code_val, etc_val1, etc_val2, etc_val3, etc_val4, etc_val5,
code_lvl, code_path, sort_seq, use_yn, reg_dtm, reg_id, upd_dtm, upd_id
]]>
  </sql>

  <!--
    * QueryID=findById
    * Description=Find one code by id
    -->
  <select id="findById" resultMap="codeBaseResultMap">
<![CDATA[
/* QueryID=kkdugi.core.code.mapper.CodeBaseMapper.findById */
SELECT
]]>
    <include refid="columns"/>
<![CDATA[
FROM kkdugi_code_base
WHERE code_id = #{id}
]]>
  </select>

  <!--
    * QueryID=findChildren
    * Description=Find direct children of a parent code with paging
    -->
  <select id="findChildren" resultMap="codeBaseResultMap">
<![CDATA[
/* QueryID=kkdugi.core.code.mapper.CodeBaseMapper.findChildren */
SELECT
]]>
    <include refid="columns"/>
<![CDATA[
FROM kkdugi_code_base cb
]]>
    <where>
      <choose>
        <when test="parentId != null">
<![CDATA[
AND cb.code_parent_id = #{parentId}
]]>
        </when>
        <otherwise>
<![CDATA[
AND cb.code_parent_id IS NULL
]]>
        </otherwise>
      </choose>
      <if test="code != null and code != ''">
<![CDATA[
AND cb.code_val LIKE '%' || #{code} || '%'
]]>
      </if>
      <if test="path != null and path != ''">
<![CDATA[
AND cb.code_path LIKE '%' || #{path} || '%'
]]>
      </if>
      <if test="use != null and use != ''">
<![CDATA[
AND cb.use_yn = #{use}
]]>
      </if>
      <if test="name != null and name != ''">
<![CDATA[
AND EXISTS (
    SELECT 1 FROM kkdugi_code_lang cl
    WHERE cl.code_id = cb.code_id AND cl.code_nm LIKE '%' || #{name} || '%'
)
]]>
      </if>
    </where>
<![CDATA[
ORDER BY cb.sort_seq NULLS LAST, cb.code_val
OFFSET #{offset} LIMIT #{pageSize}
]]>
  </select>

  <!--
    * QueryID=countChildren
    * Description=Count direct children matching the search
    -->
  <select id="countChildren" resultType="long">
<![CDATA[
/* QueryID=kkdugi.core.code.mapper.CodeBaseMapper.countChildren */
SELECT COUNT(*)
FROM kkdugi_code_base cb
]]>
    <where>
      <choose>
        <when test="parentId != null">
<![CDATA[
AND cb.code_parent_id = #{parentId}
]]>
        </when>
        <otherwise>
<![CDATA[
AND cb.code_parent_id IS NULL
]]>
        </otherwise>
      </choose>
      <if test="code != null and code != ''">
<![CDATA[
AND cb.code_val LIKE '%' || #{code} || '%'
]]>
      </if>
      <if test="path != null and path != ''">
<![CDATA[
AND cb.code_path LIKE '%' || #{path} || '%'
]]>
      </if>
      <if test="use != null and use != ''">
<![CDATA[
AND cb.use_yn = #{use}
]]>
      </if>
      <if test="name != null and name != ''">
<![CDATA[
AND EXISTS (
    SELECT 1 FROM kkdugi_code_lang cl
    WHERE cl.code_id = cb.code_id AND cl.code_nm LIKE '%' || #{name} || '%'
)
]]>
      </if>
    </where>
  </select>

  <!--
    * QueryID=findSelfAndDescendants
    * Description=Find a code and all its descendants by path prefix
    -->
  <select id="findSelfAndDescendants" resultMap="codeBaseResultMap">
<![CDATA[
/* QueryID=kkdugi.core.code.mapper.CodeBaseMapper.findSelfAndDescendants */
SELECT
]]>
    <include refid="columns"/>
<![CDATA[
FROM kkdugi_code_base
WHERE code_path = #{path} OR code_path LIKE #{path} || '/%'
]]>
  </select>

  <!--
    * QueryID=insert
    * Description=Insert one code row
    -->
  <insert id="insert" parameterType="kkdugi.core.code.models.CodeBase">
<![CDATA[
/* QueryID=kkdugi.core.code.mapper.CodeBaseMapper.insert */
INSERT INTO kkdugi_code_base
    (code_id, code_parent_id, code_val, etc_val1, etc_val2, etc_val3, etc_val4, etc_val5,
     code_lvl, code_path, sort_seq, use_yn, reg_dtm, reg_id)
VALUES
    (#{id}, #{parentId}, #{code}, #{extra1}, #{extra2}, #{extra3}, #{extra4}, #{extra5},
     #{level}, #{path}, #{sort}, #{use}, #{createdAt}, #{createdId})
]]>
  </insert>

  <!--
    * QueryID=update
    * Description=Update one code row
    -->
  <update id="update" parameterType="kkdugi.core.code.models.CodeBase">
<![CDATA[
/* QueryID=kkdugi.core.code.mapper.CodeBaseMapper.update */
UPDATE kkdugi_code_base
SET etc_val1 = #{extra1},
    etc_val2 = #{extra2},
    etc_val3 = #{extra3},
    etc_val4 = #{extra4},
    etc_val5 = #{extra5},
    sort_seq = #{sort},
    use_yn   = #{use},
    upd_dtm  = #{updatedAt},
    upd_id   = #{updatedId}
WHERE code_id = #{id}
]]>
  </update>

  <!--
    * QueryID=deleteByIds
    * Description=Delete code rows by id list
    -->
  <delete id="deleteByIds">
<![CDATA[
/* QueryID=kkdugi.core.code.mapper.CodeBaseMapper.deleteByIds */
DELETE FROM kkdugi_code_base
WHERE code_id IN
]]>
    <foreach item="id" collection="ids" open="(" separator="," close=")">
      #{id}
    </foreach>
  </delete>

</mapper>
